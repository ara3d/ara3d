using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cloner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Heeey!");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("I'm updated!");
    }
}
