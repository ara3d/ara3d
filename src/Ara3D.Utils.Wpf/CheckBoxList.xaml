<UserControl x:Class="Ara3D.Utils.Wpf.CheckBoxList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="343.712" d:DesignWidth="188.497">
    <Grid>
        <ListBox x:Name="ListBox" x:FieldModifier="public" SelectionMode="Extended">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Item1}" IsChecked="{Binding Item2}" PreviewMouseDoubleClick="OnPreviewMouseDoubleClick"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
    